## 🗄️ SQL Data Preparation

The dataset was prepared using SQL to:
- Calculate SLA breach status per ticket
- Aggregate ticket metrics by category and agent
- Compute average resolution time and SLA breach percentage

The SQL queries used for data transformation are available in the `/SQL` folder.
